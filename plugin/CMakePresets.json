{
  "version": 6,
  "cmakeMinimumRequired": { "major": 3, "minor": 24, "patch": 0 },
  "configurePresets": [
    {
      "name": "release",
      "displayName": "Release (x64)",
      "description": "The only configuration that is ever deployed or tested. Debug is generated too, so it keeps linking, but it is never shipped.",
      "generator": "Visual Studio 17 2022",
      "architecture": "x64",
      "binaryDir": "${sourceDir}/build",
      "cacheVariables": {
        "CMAKE_CONFIGURATION_TYPES": "Release;Debug"
      }
    }
  ],
  "buildPresets": [
    {
      "name": "release",
      "displayName": "Release (x64)",
      "configurePreset": "release",
      "configuration": "Release"
    },
    {
      "name": "debug",
      "displayName": "Debug (x64) - link check only, never deployed",
      "configurePreset": "release",
      "configuration": "Debug"
    }
  ]
}
